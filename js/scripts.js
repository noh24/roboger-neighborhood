// Describe beepityBoopity()

// Test1: Take string 0, return array of 0
// Code: 
// const num = "0";
// beepityBoopity(num);
// Expected Output: [0];

const num = "0";

function beepityBoopity(num) {
  let numAr = [];
  numAr.push(num);
  return numAr;
}